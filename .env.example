# Expensify Auto Login Configuration
# 
# IMPORTANT: Passwords are NEVER stored in .env files for security.
# Use macOS Keychain or interactive prompts instead.
#
# See README.md for secure credential setup instructions.

# Your Gmail address (optional - will prompt if not set)
EMAIL=your-email@gmail.com

# Expensify login URL
LOGIN_URL=https://dev.new.expensify.com:8082/

# Email sender address (usually Expensify)
FROM_EMAIL=noreply@expensify.com

# Run browser in headless mode (true/false)
HEADLESS=false
APP_PASSWORD=xxxx-xxxx 

# Disable macOS Keychain (set to "false" to always use interactive prompt)
# USE_KEYCHAIN=true
